import { render } from "@testing-library/react";
import { Navbar } from "../../src/components/Navbar/Navbar";

describe('Component test - Navbar', () => {
	test('1 plus 1 should be equal to 2', () => {
		render(<Navbar />);
		//const NavbarElement = screen.getAllByTestId();
	});
});